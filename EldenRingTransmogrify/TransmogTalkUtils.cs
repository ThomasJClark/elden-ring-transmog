using SoulsFormats;

/**
 * Utilities for updating dialogue trees (talkesd)
 */
class TransmogTalkUtils
{
    public static void AddTransmogrifyMenuOption(
        Dictionary<long, ESD.State> stateGroup,
        int transmogMessageId,
        int undoMessageId,
        int shopLineupStartId,
        int shopLineupEndId,
        int undoSpEffectId
    ) { }
}
